# Light deployment requirements for Railway (or any low-RAM host) when
# config.py has USE_HF_INFERENCE_API=true. No torch, no sentence-transformers
# — embedding + reranking happen via the Hugging Face Inference API instead
# of loading ~1.5GB of models into this process's RAM.
fastapi
uvicorn[standard]
pydantic
python-dotenv
groq
neo4j
chromadb
rank_bm25
tiktoken
langchain_text_splitters
pymupdf4llm
requests
