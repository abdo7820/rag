{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pip install -r requirements-railway.txt"
  },
  "deploy": {
    "startCommand": "/app/.venv/bin/uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1"
  }
}
